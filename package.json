{
	"private": true,
	"workspaces": [
		"@arkengine/*",
		"@types/*"
	],
	"scripts": {
		"test": "yarn workspaces foreach -v run test",
		"build": "yarn workspaces foreach -v run build",
		"clean": "yarn workspaces foreach -v run clean",
		"pkg-new": "node scripts/pkg-new.js",
		"ts-link": "yarn workspaces foreach run ts-link",
		"report": "xdg-open report/coverage/lcov-report/index.html"
	},
	"devDependencies": {
		"@types/node": "^14.14.6",
		"@typescript-eslint/eslint-plugin": "^4.7.0",
		"@typescript-eslint/parser": "^4.7.0",
		"@yarnpkg/pnpify": "^2.0.0-rc.22",
		"eslint": "^7.13.0",
		"eslint-config-prettier": "^6.15.0",
		"eslint-plugin-simple-import-sort": "^6.0.1",
		"prettier": "^2.0.5",
		"reflect-metadata": "^0.1.13",
		"shelljs": "^0.8.4",
		"typescript": "^4.0.5"
	}
}
